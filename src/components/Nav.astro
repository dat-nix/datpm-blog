---
const currentPath = Astro.url.pathname;
const links = [
  { href: '/writing', label: 'writing' },
  { href: '/ctf', label: 'ctf' },
  { href: '/projects', label: 'projects' },
  { href: '/about', label: 'about' },
];
---
<style>
  .nav {
    border-bottom: 2px solid var(--line);
    padding: 12px 18px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 14px;
  }
  .nav-name {
    font-size: 14px;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    text-decoration: none;
    background: none !important;
    color: var(--ink) !important;
    white-space: nowrap;
  }
  .nav-links {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: 6px;
  }
  .nav-links a {
    font-size: 12px;
    text-decoration: none;
    border: 1px solid var(--line);
    padding: 4px 9px;
    letter-spacing: 0.04em;
    background: var(--paper);
    box-shadow: 2px 2px 0 var(--line);
    transition: transform 120ms ease, box-shadow 120ms ease, background 120ms ease;
  }
  .nav-links a:hover {
    transform: translate(1px, 1px);
    box-shadow: 1px 1px 0 var(--line);
  }
  .nav-links a.active {
    background: var(--ink);
    color: var(--paper);
  }
  .marquee-bar {
    background: var(--ink);
    color: var(--paper);
    font-family: var(--mono);
    font-size: 11px;
    padding: 5px 0;
    overflow: hidden;
    white-space: nowrap;
    border-bottom: 2px solid var(--line);
  }
  .marquee-inner {
    display: inline-block;
    animation: marquee 22s linear infinite;
    padding-left: 12px;
  }
  @keyframes marquee {
    from { transform: translateX(0); }
    to   { transform: translateX(-50%); }
  }
  @media (prefers-reduced-motion: reduce) {
    .marquee-inner { animation: none; }
  }
  @media (max-width: 560px) {
    .nav {
      align-items: stretch;
      flex-direction: column;
      padding: 12px;
    }
    .nav-links {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      justify-content: stretch;
    }
    .nav-links a { text-align: center; }
  }
</style>

<nav class="nav">
  <a href="/" class="nav-name">datpm.com</a>
  <div class="nav-links">
    {links.map(l => (
      <a href={l.href} class={currentPath.startsWith(l.href) ? 'active' : ''}>
        {l.label}
      </a>
    ))}
  </div>
</nav>

<div class="marquee-bar">
  <span class="marquee-inner">
    ★ currently: grinding binary exploitation &nbsp;&nbsp;&nbsp;
    ★ target: first bug bounty report &nbsp;&nbsp;&nbsp;
    ★ building in public &nbsp;&nbsp;&nbsp;
    ★ RMIT Vietnam · IT Year 2 &nbsp;&nbsp;&nbsp;
    ★ inspired by geohot &amp; orange tsai &nbsp;&nbsp;&nbsp;
    ★ no cookies · no tracking · no bullshit &nbsp;&nbsp;&nbsp;
    ★ currently: grinding binary exploitation &nbsp;&nbsp;&nbsp;
    ★ target: first bug bounty report &nbsp;&nbsp;&nbsp;
    ★ building in public &nbsp;&nbsp;&nbsp;
    ★ RMIT Vietnam · IT Year 2 &nbsp;&nbsp;&nbsp;
    ★ inspired by geohot &amp; orange tsai &nbsp;&nbsp;&nbsp;
    ★ no cookies · no tracking · no bullshit &nbsp;&nbsp;&nbsp;
  </span>
</div>
